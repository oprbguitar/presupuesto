# El pipeline usa solo la biblioteca estándar de Python 3.9+ (urllib, csv, json).
# No requiere dependencias externas para funcionar.
# Descomente si en el futuro incorpora estas utilidades:
# requests>=2.31
# ijson>=3.2        # streaming de paquetes OCDS muy grandes
# openpyxl>=3.1     # lectura de exportaciones .xlsx del MEF
